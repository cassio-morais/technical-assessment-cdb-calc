export const environment = {
    production: false,
    baseUrl: 'http://localhost:8081/api', // base url docker
    // locally without docker you can change the baseurl to 'http://localhost:5139/api'
  };